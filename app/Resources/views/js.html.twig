 <script>
	let liveSearchUrl = "{{ path('live_search', {'what': '__what__'}) }}";
	let entityUrl = "{{ path('entity_locale', {'what': '__what__', id: '__id__'}) }}";
	let saveUrl = "{{ path('entity_save', {'what': '__what__', id: '__id__'}) }}" ;
 </script>